import React from "react";

const Formules = () => {
  return (
    <div className="relative h-[50vh] md:h-screen w-full bg-formules bg-cover bg-center p-6">
      <div class="absolute inset-0 bg-black bg-opacity-30">
        <div className="px-8  flex justify-center items-center h-full">
          <h1 className="text-white font-bold text-[40px]">NOS FORMULES</h1>
        </div>
      </div>
    </div>
  );
};

export default Formules;
